version: '3.8'

services:
  lettia-automation:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lettia-automation
    volumes:
      - ./.env:/app/.env:ro
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

